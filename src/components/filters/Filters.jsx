import React from "react";

const Filters = () => {

    return <div>
        <input placeholder="Buscar productos por nombre"></input>
        <span>Ordenar por</span>
        <select>
            <option>Seleccionar</option>
        </select>
    </div>
};

export default Filters;